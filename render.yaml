services:
  - type: web
    name: telegram-sfl-bot
    runtime: python
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: python sfl.py
    autoDeploy: false
    envVars:
      - key: BOT_TOKEN
        sync: false